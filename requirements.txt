livekit-agents==1.6.8
livekit-plugins-speechmatics==1.6.8
livekit-plugins-google==1.6.8
livekit-plugins-silero==1.6.8
livekit-plugins-noise-cancellation==0.2.6
livekit-plugins-ai-coustics==0.3.0
nara_wpe==0.0.11
google-genai>=2.0.0
supabase>=2.27.2
httpx>=0.27.0
aiohttp>=3.10.0
python-dotenv>=1.2.1
livekit-plugins-openai==1.6.8
# NOTE: durable voice-identity deps (ECAPA-TDNN) are intentionally NOT here —
# they live in requirements-voice-identity.txt so the heavy ~1-2GB torch
# pull is a DELIBERATE deploy opt-in (after Doc applies migrations/021), not
# forced into every image. The embedder lazy-loads and no-ops without them,
# so the feature stays inert until that file is installed.
